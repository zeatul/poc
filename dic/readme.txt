文件数据库
数据字典 ，项目基石

数据库 ：mysql,oracle
数据类型：Integer,Long ,String ,Date, BigDecimal
技术元数据：英文名, 中文名,描述 ，类型，校验规则，规则传播给service和前端校验
业务元数据：英文名，中文名，描述 ，规则

数据库元数据：Database，table ，view ， pk ， index ,sql(根据sql 生成视图),tools(根据元数据生成sql）,数据库版本


设定业务元数据字段，供建表选择。

根据建表模型，生成建表语句，生成domian,生成sql map，生成dos，生成service。


数据维护：

单表维护，自动生成 所有代码，校验，查询，分页，下载，导出，增加，删除，修改，html，js。

树形结构单表数据维护

主从表数据维护 ，主从表校验，根据主表key，查字表，根据主表key，删除字表。 （如何，增加业务校验规则)


业务校验规则维护：如何表述，如何执行。
权限规则维护：如何表述，如何执行。

业务人员根据规则，在特定业务需求下，直接根据 元数据，生成直接可用的项目。

业务规则定义： 流程引擎，表单设计。

